import { n, m, q } from '../core/util.js';;/** * Servicio de Conversión entre presentaciones * Maneja la lógica de cajas ↔ unidades sueltas */export const ConversionService = {  /**   * Descompone stock total en cajas completas y unidades sueltas   */  descomponerStock: function (stockTotal, unidadesPorCaja) {    const total = n(stockTotal);    const porCaja = n(unidadesPorCaja);    if (porCaja <= 0) return { cajas: 0, unidades: total };    const cajas = Math.floor(total / porCaja);    const unidades = q(total - (cajas * porCaja));    return { cajas, unidades };  },  /**   * Convierte unidades a cajas (puede ser decimal)   */  unidadesACajas: function (unidades, unidadesPorCaja) {    const u = n(unidades);    const c = n(unidadesPorCaja);    if (c <= 0) return 0;    return q(u / c);  },  /**   * Convierte cajas a unidades totales   */  cajasAUnidades: function (cajas, unidadesPorCaja) {    return m(n(cajas) * n(unidadesPorCaja));  },  /**   * Verifica si se puede vender una cantidad dada   */  puedeVender: function (cantidadSolicitada, stockVariante, stockUnidadSuelta, unidadesPorCaja, esCaja) {    const req = n(cantidadSolicitada);    if (esCaja) {      return req <= n(stockVariante);    }    const stockUni = n(stockUnidadSuelta);    const stockCajas = n(stockVariante);    const totalDisponible = stockUni + (stockCajas * n(unidadesPorCaja));    return req <= totalDisponible;  },  /**   * Calcula costo promedio ponderado de una variante   */  costoPromedioVariante: function (lotes, varianteId) {    const lotesVar = lotes.filter(l => l.varianteId === varianteId && (n(l.cantidadInicial) - n(l.cantidadVendida)) > 0);    let totalCosto = 0, totalCant = 0;    for (const l of lotesVar) {      const disp = n(l.cantidadInicial) - n(l.cantidadVendida);      totalCosto += disp * n(l.costo);      totalCant += disp;    }    return totalCant > 0 ? m(totalCosto / totalCant) : 0;  },};